<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Тема для splash-экрана в темном режиме -->
    <style name="App.Starting.Theme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_icon_dark</item>
        <item name="postSplashScreenTheme">@style/Theme.NewsApp</item>
    </style>

    <!-- Основная тема приложения в темном режиме -->
    <style name="Theme.NewsApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>